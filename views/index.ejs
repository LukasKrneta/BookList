<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BookList</title>
</head>
<body>
    
    <form action="/new" method="POST">
        <input type="text" name="title" placeholder="Title">
        <textarea name="notes" placeholder="Notes"></textarea>
        <input type="date" name="date" placeholder="Date when finished reading">
        <input type="submit">
    </form>
    
    <% if(locals.title && locals.author && locals.notes && locals.date && locals.cover){ %>
    <h1><%= title %></h1>
    <h2><%= author %></h2>
    <p><%= notes %></p>
    <p><b><%= date %></b></p>
    <img src="<%= cover %>" alt="image">

    <% } %>

</body>
</html>